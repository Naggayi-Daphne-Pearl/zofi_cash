import React from "react";
import ResetPassword from '../../components/v1/ResetPasswordForm';

const resetpassword = () => {
  return (
    <div>
      <ResetPassword />
    </div>
  );
};

export default resetpassword;
